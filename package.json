{
  "name": "svelvet-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production svelvet --preprocess preprocess.js",
    "clean": "rm -rf dist",
    "dev": "svelvet --preprocess preprocess.js",
    "reinstall": "rm -rf node_modules package-lock.json && npm install"
  },
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.11.1",
    "lodash": "^4.17.19",
    "svelte": "^3.20.1"
  },
  "devDependencies": {
    "babel-plugin-inline-json-import": "^0.3.2",
    "svelte-preprocess": "^3.7.1",
    "svelvet": "^0.4.1",
    "typescript": "^3.8.3"
  }
}
